package com.example.sbabb.facial;

public class AddPersonFragment {
    // from "add new person button" or "edit person"
    // takes in user
    // if user is not null, editable fields are populated
    // if user is null (navigated to from "add new person but"), displays blank imageView and fields
    // when the user exits/ presses save, it will update the image and User in FB
}
